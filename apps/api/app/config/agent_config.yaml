agents:
  intake:
    enabled: true
    execution_mode: background
    timeout_seconds: 30
    retries: 1
  document_intelligence:
    enabled: true
    execution_mode: background
    timeout_seconds: 60
    retries: 2
  entity_resolution:
    enabled: true
    execution_mode: background
    timeout_seconds: 45
    retries: 1
  sanctions_compliance:
    enabled: true
    execution_mode: background
    timeout_seconds: 60
    retries: 2
  financial_risk:
    enabled: true
    execution_mode: background
    timeout_seconds: 60
    retries: 2
  news_reputation:
    enabled: true
    execution_mode: background
    timeout_seconds: 60
    retries: 2
  scoring:
    enabled: true
    execution_mode: sync_or_background
    timeout_seconds: 15
    retries: 1
  recommendation:
    enabled: true
    execution_mode: sync_or_background
    timeout_seconds: 20
    retries: 1
  human_review:
    enabled: true
    execution_mode: sync_or_background
    timeout_seconds: 20
    retries: 1
